{
  "private": true,
  "name": "applause-worker",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Florian Klampfer <mail@qwtel.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "dependencies": {
    "base64-encoding": "^0.10.0",
    "faunadb": "^3.0.1",
    "sanitize-html": "^1.27.3",
    "uuid-class": "^0.9.0"
  }
}
